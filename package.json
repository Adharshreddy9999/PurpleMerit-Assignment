{
  "name": "purplemerit-backend",
  "private": true,
  "workspaces": ["api", "ws", "worker", "shared"],
  "scripts": {
    "dev:api": "npm --workspace api run dev",
    "dev:ws": "npm --workspace ws run dev",
    "dev:worker": "npm --workspace worker run dev"
  }
}
